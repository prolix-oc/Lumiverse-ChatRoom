{
  "name": "lumiverse-chatroom",
  "version": "1.0.0",
  "description": "Live Internet Shitposting Chatroom",
  "main": "index.js",
  "scripts": {
    "build": "bun build src/backend.ts --outfile=dist/backend.js --target=browser && bun build src/frontend.ts --outfile=dist/frontend.js --target=browser"
  },
  "dependencies": {
    "@tanstack/virtual-core": "^3.14.0",
    "lumiverse-spindle-types": "0.5.17"
  }
}
